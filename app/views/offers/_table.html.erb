<table data-controller="datatable" 
       data-datatable-name-value="offers"
       data-datatable-request-value="offers/table"
>
  <thead>
    <tr>
      <% columns.each do |column| %>
        <th><%= t(column) %></th>
      <% end %>
    </tr>
  <thead>
</table>
