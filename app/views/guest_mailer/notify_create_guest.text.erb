Bonjour,

<% username = (@user.name.nil? || @user.surname.nil?) ? 'Un contact' : "#{@user.surname} #{@user.name}" %>

<%= mail_to @user.email, username %> vous partage l'accès à liste de ses parcelles.

Rendez-vous sur votre <%= link_to 'liste', parcelles_url %>.

Bonne récolte
L'équipe GP
