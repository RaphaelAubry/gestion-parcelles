<%= render partial: 'shared/table', locals: { instances: @people,
                                              controller: :invitations,
                                              options: {
                                                columns: User::Guest_INSTANCE_VARIABLES,
                                                formulas: [],
                                                link_show: false
                                                }
                                            }
%>
<link-group>
  <%= link_to 'inviter', new_invitation_path %>
</link-group>
