<table data-controller="datatable" 
       data-datatable-name-value="finances/invoices"
       data-datatable-request-value="finances/invoices/table"
>
  <thead>
    <tr>
      <% columns.each do |column| %>
        <th><%= t(column) %></th>
      <% end %>
    </tr>
  <thead>
</table>