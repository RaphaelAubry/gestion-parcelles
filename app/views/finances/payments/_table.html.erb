<table data-controller="datatable" 
       data-datatable-name-value="finances/payments"
       data-datatable-request-value="finances/payments/table"
>
  <thead>
    <tr>
      <% columns.each do |column| %>
        <th><%= t(column) %></th>
      <% end %>
    </tr>
  <thead>
</table>