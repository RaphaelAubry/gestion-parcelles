<table data-controller="datatable" 
       data-datatable-name-value="parcelles"
       data-datatable-request-value="parcelles/table" 
       data-datatable-user-id-value="<%= user_id %>"
       data_datatable-column-action-value="<%=  %>"
>
  <thead>
    <tr>
      <% columns.each do |column| %>
        <th><%= t(column) %></th>
      <% end %>
    </tr>
  <thead>
</table>